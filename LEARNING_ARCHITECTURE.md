# 云昭智能学习中枢 · v1.1

## 目标

云昭的“学习”不是修改自身程序、绕过权限或把模型输出永久当事实，而是建立一个**用户可查看、可固定、可删除、可一键关闭**的本地长期记忆层，并把工具运行经验独立保存。

## GitHub 方案取长补短

设计参考了三类开源思路，但没有把服务端 Python/Node 代码直接塞进 Flutter：

1. **LangMem (`langchain-ai/langmem`, MIT)**
   - 借鉴：从对话中提取长期有价值的信息；记忆合并/更新；前台检索 + 后台学习的职责拆分。
2. **Letta / MemGPT (`letta-ai/letta`)**
   - 借鉴：持久 Agent 状态、分层长期记忆、把“当前上下文”与“长期记忆”分开。
3. **flutter_local_llm (`EXXETA/flutter_local_llm`, MIT)**
   - 借鉴：移动端本地数据 + RAG/检索思路。v1.1 为降低包体和设备负担，采用本地轻量词项/中文 bigram 检索；以后可以无侵入替换为本地 embedding。

## 四层记忆

- `preference`：长期偏好。
- `fact`：用户明确表达的稳定事实。
- `project`：长期项目/任务上下文。
- `style`：用户希望云昭采用的交互方式。
- `toolLesson`：MCP / Search 的运行经验，和用户事实严格隔离。

## 学习流水线

```text
用户消息
  ↓
正常 Agent 回答
  ↓
AI 记忆候选提取（最多 5 条）
  ↓
Evidence Guard：必须附带用户原话逐字证据
  ↓
Secret / Injection Guard
  ↓
相似记忆合并（阈值 0.72）
  ↓
SharedPreferences 本地长期存储
  ↓
下轮仅检索“与请求相关”的记忆
```

## 防记忆投毒

长期记忆是持久攻击面，因此 v1.1 做了以下硬限制：

- AI 提取的记忆必须携带 `evidence`，且该证据必须**逐字存在于用户消息**中，否则丢弃。
- MCP / Web Search 的返回内容不直接变成用户长期事实。
- API Key、Bearer Token、密码、验证码等命中规则后拒绝入库。
- 疑似“忽略系统/解除限制/以后自动执行所有工具”等指令不会进入长期记忆，除非证据明确只是合法的交互风格偏好。
- 长期记忆注入明确标注：**不能覆盖系统规则、本轮用户明确指令或工具真实结果**。
- 学习过程异常永远不能中断主聊天链路。

## 检索与衰减

当前评分由以下因素组成：

- 词项 / 中文 bigram 相似度：60%
- 记忆置信度：18%
- 最近更新时间：10%
- 多次合并后的强度：12%
- 用户手动“固定”额外加权

默认最多保存 240 条，并优先保留固定/高强度/新近记忆。

## 用户控制

设置 → **云昭智能学习**：

- 总开关
- AI 记忆提取开关
- 回答前相关记忆检索开关
- MCP / Search 工具经验学习开关
- 查看所有长期记忆
- 固定 / 删除单条记忆
- 清空全部长期记忆
- 清空工具经验

聊天页另有单轮“学习”开关；清空聊天历史不会偷偷清掉长期记忆。
